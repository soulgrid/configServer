server:
  port: 50001

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/soulgrid/Configs
          default-label: master

#management:
#  endpoints:
#    web:
#      exposure:
#        include: health, busrefresh
